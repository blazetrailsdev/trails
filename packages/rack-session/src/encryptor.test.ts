import { it } from "vitest";

it.skip("initialize does not destroy key string", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("initialize raises ArgumentError on invalid key", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("initialize raises ArgumentError on short key", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypts an encrypted message", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypt raises InvalidSignature for tampered messages", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypts an encrypted message with purpose", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypts raises InvalidSignature without purpose", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypts raises InvalidSignature with different purpose", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("initialize raises ArgumentError on invalid pad_size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("initialize raises ArgumentError on to short pad_size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("initialize raises ArgumentError on to long pad_size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("decrypts an encrypted message without pad_size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("encryptor with pad_size increases message size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("encryptor with pad_size has message payload size to multiple of pad_size", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("uses a secret cipher key for encryption and decryption", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
it.skip("it calls set_cipher_key with the correct key", () => {
  // PERMANENT-SKIP: RFC 0133 non-goal — Encryptor serves only Rack::Session::Cookie, which Rails CookieStore does not use
});
