import { describe, it } from "vitest";

describe("DelegationTest", () => {
  it.skip("not respond to arel method", () => {
    /* TODO: needs helpers from original file */
  });

  it.skip("delegate querying methods", () => {
    /* TODO: needs helpers from original file */
  });

  it.skip("delegation doesn't override methods defined in other relation subclasses", () => {
    /* TODO: needs helpers from original file */
  });
});
