import { describe, it } from "vitest";

describe("TestFixturesTest", () => {
  it.skip("use transactional tests defaults to true", () => {});
  it.skip("use transactional tests can be overridden", () => {});
  it.skip("inclusion runs active record fixtures load hook", () => {});
  it.skip("doesnt rely on active support test case specific methods", () => {});
});
