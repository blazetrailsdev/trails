import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::KeyTest", () => {
  it.skip("A key can store a secret and public tags", () => {});
  it.skip(".derive_from instantiates a key with its secret derived from the passed password", () => {});
});
