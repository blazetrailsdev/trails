import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::NullEncryptorTest", () => {
  it.skip("encrypt returns the passed data", () => {});
  it.skip("decrypt returns the passed data", () => {});
  it.skip("encrypted? returns false", () => {});
});
