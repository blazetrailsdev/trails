import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::MessageTest", () => {
  it.skip("add_header lets you add headers", () => {});
  it.skip("add_headers lets you add multiple headers", () => {});
  it.skip("headers can't be overridden", () => {});
  it.skip("validates that payloads are either nil or strings", () => {});
});
