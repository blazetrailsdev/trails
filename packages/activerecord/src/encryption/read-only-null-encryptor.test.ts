import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::ReadOnlyNullEncryptorTest", () => {
  it.skip("decrypt returns the encrypted message", () => {});
  it.skip("encrypt raises an Encryption", () => {});
});
