import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::EnvelopeEncryptionKeyProviderTest", () => {
  it.skip("encryption_key returns random encryption keys", () => {});
  it.skip("generate_random_encryption_key generates keys of 32 bytes", () => {});
  it.skip("generated random keys carry their secret encrypted with the primary key", () => {});
  it.skip("decryption_key_for returns the decryption key for a message that was encrypted with a generated encryption key", () => {});
  it.skip("work with multiple keys when config.store_key_references is false", () => {});
  it.skip("work with multiple keys when config.store_key_references is true", () => {});
});
