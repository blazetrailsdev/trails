import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::Aes256GcmTest", () => {
  it.skip("encrypts strings", () => {});
  it.skip("works with empty strings", () => {});
  it.skip("uses non-deterministic encryption by default", () => {});
  it.skip("in deterministic mode, it generates the same ciphertext for the same inputs", () => {});
  it.skip("it generates different ivs for different ciphertexts", () => {});
  it.skip("inspect_does not show secrets", () => {});
});
