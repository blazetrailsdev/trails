import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::EncryptingOnlyEncryptorTest", () => {
  it.skip("decrypt returns the passed data", () => {});
  it.skip("encrypt encrypts the passed data", () => {});
});
