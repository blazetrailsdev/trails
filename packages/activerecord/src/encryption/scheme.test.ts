import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::SchemeTest", () => {
  it.skip("validates config options when using encrypted attributes", () => {});
  it.skip("should create a encryptor well when compressor is given", () => {});
  it.skip("should create a encryptor well when compress is false", () => {});
});
