import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::DeterministicKeyProviderTest", () => {
  it.skip("will raise a configuration error when trying to configure multiple keys", () => {});
});
