import { describe, it } from "vitest";

describe("ActiveRecord::Encryption::KeyGeneratorTest", () => {
  it.skip("generate_random_key generates random keys with the cipher key length by default", () => {});
  it.skip("generate_random_key generates random keys with a custom length", () => {});
  it.skip("generate_random_hex_key generates random hexadecimal keys with the cipher key length by default", () => {});
  it.skip("generate_random_hex_key generates random hexadecimal keys with a custom length", () => {});
  it.skip("derive keys using the configured digest algorithm", () => {});
  it.skip("derive_key derives a key with from the provided password with the cipher key length by default", () => {});
  it.skip("derive_key derives a key with a custom length", () => {});
});
