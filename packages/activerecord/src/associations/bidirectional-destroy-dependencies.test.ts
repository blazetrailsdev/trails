import { describe, it } from "vitest";

describe("BidirectionalDestroyDependenciesTest", () => {
  it.skip("bidirectional dependence when destroying item with belongs to association", () => {
    /* TODO: needs helpers from original file */
  });

  it.skip("bidirectional dependence when destroying item with has one association", () => {
    /* TODO: needs helpers from original file */
  });

  it.skip("bidirectional dependence when destroying item with has one association fails first time", () => {
    /* TODO: needs helpers from original file */
  });
});
