import { describe, it } from "vitest";

describe("CacheEntryTest", () => {
  it.skip("expired", () => {
    /* fixture-dependent */
  });
  it.skip("initialize with expires at", () => {
    /* fixture-dependent */
  });
});
