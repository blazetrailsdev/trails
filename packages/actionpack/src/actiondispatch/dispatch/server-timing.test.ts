import { describe, it } from "vitest";

describe("ServerTimingTest", () => {
  it.skip("server timing header is included in the response", () => {});
  it.skip("includes default action controller events duration", () => {});
  it.skip("includes custom active support events duration", () => {});
  it.skip("events are tracked by thread", () => {});
  it.skip("does not overwrite existing header values", () => {});
});
