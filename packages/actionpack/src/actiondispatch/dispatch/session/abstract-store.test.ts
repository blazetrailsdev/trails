import { describe, it } from "vitest";

describe("AbstractStoreTest", () => {
  it.skip("session is set", () => {});
  it.skip("new session object is merged with old", () => {});
  it.skip("update raises an exception if arg not hashable", () => {});
});
