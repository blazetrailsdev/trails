import { describe, it } from "vitest";

describe("ServerTest", () => {
  it.skip("port is always included", () => {});
  it.skip("server is changed from `default` to `puma`", () => {});
  it.skip("server is not changed to `puma` when is different than default", () => {});
});
