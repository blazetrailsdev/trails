import { describe, it } from "vitest";

describe("ScreenshotHelperTest", () => {
  it.skip("image path is saved in tmp directory", () => {});
  it.skip("image path unique counter is changed when incremented", () => {});
  it.skip("image path unique counter generates different path in same test", () => {});
  it.skip("image path uses the Capybara.save_path to set a custom directory", () => {});
  it.skip("image path includes failures text if test did not pass", () => {});
  it.skip("image path does not include failures text if test skipped", () => {});
  it.skip("image name truncates names over 225 characters including counter", () => {});
  it.skip("defaults to simple output for the screenshot", () => {});
  it.skip("take_screenshot saves HTML and shows link to it when using RAILS_SYSTEM_TESTING_SCREENSHOT_HTML env", () => {});
  it.skip("take_screenshot saves HTML and shows link to it when using html: kwarg", () => {});
  it.skip("take_screenshot allows changing screenshot display format via RAILS_SYSTEM_TESTING_SCREENSHOT env", () => {});
  it.skip("take_screenshot allows changing screenshot display format via screenshot: kwarg", () => {});
  it.skip("take_failed_screenshot persists the image path in the test metadata", () => {});
  it.skip("image path returns the absolute path from root", () => {});
  it.skip("Non word characters are replaced with dashes in paths", () => {});
});

describe("RackTestScreenshotsTest", () => {
  it.skip("rack_test driver does not support screenshot", () => {});
});

describe("SeleniumScreenshotsTest", () => {
  it.skip("selenium driver supports screenshot", () => {});
});
