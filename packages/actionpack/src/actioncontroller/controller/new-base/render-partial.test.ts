import { describe, it } from "vitest";

describe("TestPartial", () => {
  it.skip("rendering a partial in ActionView doesn't pull the ivars again from the controller", () => {});
  it.skip("rendering a template with renders another partial with other format that renders other partial in the same format", () => {});
});

describe("TestInheritedPartial", () => {
  it.skip("partial from parent controller gets picked if missing in child one", () => {});
  it.skip("partial from child controller gets picked", () => {});
});
