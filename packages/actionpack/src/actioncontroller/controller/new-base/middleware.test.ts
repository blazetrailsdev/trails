import { describe, it } from "vitest";

describe("TestMiddleware", () => {
  it.skip("middleware that is 'use'd is called as part of the Rack application", () => {});
  it.skip("the middleware stack is exposed as 'middleware' in the controller", () => {});
  it.skip("middleware stack accepts block arguments", () => {});
  it.skip("middleware stack accepts only and except as options", () => {});
});
