import { describe, it } from "vitest";

describe("OutputEscapingTest", () => {
  it.skip("escape_html shouldn't die when passed nil", () => {});
  it.skip("escapeHTML should escape strings", () => {});
  it.skip("escapeHTML shouldn't touch explicitly safe strings", () => {});
});
