import { describe, it } from "vitest";

describe("LoggingTest", () => {
  it.skip("logging at the default level", () => {});
  it.skip("logging at a noisier level per request", () => {});
  it.skip("logging at a quieter level per request", () => {});
});
