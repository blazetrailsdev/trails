import { describe, it } from "vitest";

describe("AlwaysPermittedParametersTest", () => {
  it.skip("returns super on missing constant other than NEVER_UNPERMITTED_PARAMS", () => {});
  it.skip("allows both explicitly listed and always-permitted parameters", () => {});
});
