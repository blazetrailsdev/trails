import { describe, it } from "vitest";

describe("ActionControllerPermittedParamsTest", () => {
  it.skip("parameters are forbidden", () => {});
  it.skip("parameters can be permitted and are then not forbidden", () => {});
});
