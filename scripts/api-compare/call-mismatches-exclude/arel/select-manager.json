[
  {
    "package": "arel",
    "tsFile": "select-manager.ts",
    "rubyName": "collapse",
    "call": "sql",
    "reason": "select_manager.rb:52-58 / :257-268 wrap through `Arel.sql`, which lives in the package entry index.ts; importing it here adds a select-manager→index edge that makes `table.js` throw `Cannot access 'Table' before initialization` when entered as a module (verified against built dist). The one-line body `new SqlLiteral(...)` is inlined instead."
  },
  {
    "package": "arel",
    "tsFile": "select-manager.ts",
    "rubyName": "initialize",
    "call": "new",
    "kind": "args",
    "rubyArgs": [
      "ref:table"
    ],
    "reason": "Baseline (RFC 0095): call-ARGUMENT shape divergence seeded when the argument ratchet landed — the port makes the call Rails makes with a different argument count, order, literal value or kwarg key; pending per-body convergence review."
  },
  {
    "package": "arel",
    "tsFile": "select-manager.ts",
    "rubyName": "lock",
    "call": "sql",
    "reason": "select_manager.rb:52-58 / :257-268 wrap through `Arel.sql`, which lives in the package entry index.ts; importing it here adds a select-manager→index edge that makes `table.js` throw `Cannot access 'Table' before initialization` when entered as a module (verified against built dist). The one-line body `new SqlLiteral(...)` is inlined instead."
  }
]
