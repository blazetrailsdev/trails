[
  {
    "package": "arel",
    "tsFile": "visitors/visitor.ts",
    "rubyName": "dispatch_cache",
    "call": "new",
    "kind": "args",
    "rubyArgs": [],
    "reason": "visitor.rb:18 `Hash.new { |hash, klass| hash[klass] = :\"visit_#{...}\" }.compare_by_identity` — a JS `Map`/`WeakMap` constructor takes no default block, so the miss path lives in `visit`'s prototype-chain walk instead. Language shortcoming."
  }
]
