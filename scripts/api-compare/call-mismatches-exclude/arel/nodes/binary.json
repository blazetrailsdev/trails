[
  {
    "package": "arel",
    "tsFile": "nodes/binary.ts",
    "rubyName": "to_cte",
    "call": "new",
    "kind": "args",
    "rubyArgs": [
      "ref:name",
      "ref:right"
    ],
    "reason": "binary.rb:44 `Arel::Nodes::Cte.new(left.name, right)` — importing `Cte` from `binary.ts` closes a module cycle, so the constructor arrives through the `_registerCteFactory` slot and is called as `cteFactory(name, right)` with the same two arguments. The `new` the comparator sees in this body is the registry guard's Error."
  }
]
