[
  {
    "package": "actiondispatch",
    "tsFile": "middleware/server-timing.ts",
    "rubyName": "collect_events",
    "call": "delete",
    "reason": "Pre-existing divergence in `Subscriber`, a class nested inside a same-file parent (middleware/server_timing.rb). parity:api dropped such classes from the Rails population entirely until compare-drops-nested-class-methods-from-coverage-denominator, so this body was never measured; the row records existing debt, not new code. Converge under converge-nested-class-call-mismatches-surfaced-by-population-fix."
  }
]
