[
  {
    "package": "actiondispatch",
    "tsFile": "middleware/host-authorization.ts",
    "rubyName": "allows?",
    "call": "any?",
    "reason": "Pre-existing divergence in `Permissions`, a class nested inside a same-file parent (middleware/host_authorization.rb). parity:api dropped such classes from the Rails population entirely until compare-drops-nested-class-methods-from-coverage-denominator, so this body was never measured; the row records existing debt, not new code. Converge under converge-nested-class-call-mismatches-surfaced-by-population-fix."
  },
  {
    "package": "actiondispatch",
    "tsFile": "middleware/host-authorization.ts",
    "rubyName": "response_body",
    "call": "new",
    "reason": "Pre-existing divergence in `DefaultResponseApp`, a class nested inside a same-file parent (middleware/host_authorization.rb). parity:api dropped such classes from the Rails population entirely until compare-drops-nested-class-methods-from-coverage-denominator, so this body was never measured; the row records existing debt, not new code. Converge under converge-nested-class-call-mismatches-surfaced-by-population-fix."
  }
]
