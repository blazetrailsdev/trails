[
  {
    "package": "actiondispatch",
    "tsFile": "middleware/remote-ip.ts",
    "rubyName": "ips_from",
    "call": "new",
    "reason": "Pre-existing divergence in `GetIp`, a class nested inside a same-file parent (middleware/remote_ip.rb). parity:api dropped such classes from the Rails population entirely until compare-drops-nested-class-methods-from-coverage-denominator, so this body was never measured; the row records existing debt, not new code. Converge under converge-nested-class-call-mismatches-surfaced-by-population-fix."
  }
]
