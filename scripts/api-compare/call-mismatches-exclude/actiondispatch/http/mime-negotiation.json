[
  {
    "package": "actiondispatch",
    "tsFile": "http/mime-negotiation.ts",
    "rubyName": "formats",
    "call": "set_header",
    "kind": "args",
    "rubyArgs": [
      "ref:k",
      "ref:v"
    ],
    "reason": "Extractor residue: Ruby's reader `formats` and writer `formats=` share the TS name `formats`, so the comparator reads the class's `set formats` (which mirrors mime_negotiation.rb:137's `set_header `action_dispatch.request.formats`, extensions.collect { … }`) against the READER's `set_header k, v` at mime_negotiation.rb:85. The reader function itself already forwards (k, v)."
  },
  {
    "package": "actiondispatch",
    "tsFile": "http/mime-negotiation.ts",
    "rubyName": "variant",
    "call": "new",
    "kind": "args",
    "rubyArgs": [],
    "reason": "Extractor residue, same reader/writer name collision as the `formats` row above: Ruby's `variant` reader (mime_negotiation.rb:100) is compared against the TS `set variant`, whose `new` builds the error that `variant=` raises at mime_negotiation.rb:96."
  }
]
