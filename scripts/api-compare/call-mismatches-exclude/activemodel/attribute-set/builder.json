[
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "assign_default_value",
    "call": "fetch",
    "reason": "builder.rb:166,168 `additional_types.fetch(name, types[name])` and `values.fetch(name) { value_present = false }`; a JS Map has no two-argument `fetch` and neither Map nor a plain object has a block-taking `fetch`, so the lookup is `get(name) ?? types.get(name)` and the key-presence probe is `Object.hasOwn` (same gap as the `default_attribute` -> `fetch` row)."
  },
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "attributes",
    "call": "each_key",
    "reason": "builder.rb:62-63 walks `values.each_key`/`types.each_key`; `values` is a plain object and `types` a Map here, neither of which has Ruby's Hash#each_key — `Object.keys(...)`/`Map#keys()` is the only spelling (same gap as the `materialize` -> `each_key` row below)."
  },
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "deep_dup",
    "call": "transform_values",
    "reason": "builder.rb:120 `delegate_hash.transform_values(&:dup)`; the delegate hash is a JS Map, which has no `transform_values`, and `LazyAttributeHash#transformValues` cannot stand in because it materializes first — so the copy loop is spelled inline."
  },
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "default_attribute",
    "call": "fetch",
    "reason": "builder.rb:74 `additional_types.fetch(name, types[name])`; a JS Map has no two-argument `fetch`, and no type is ever stored as null, so `get(name) ?? types.get(name)` is exact (same gap as the `assign_default_value` -> `fetch` row)."
  },
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "fetch_value",
    "call": "fetch",
    "reason": "builder.rb:46,51 `@casted_values.fetch(name) { ... }` and `values.fetch(name) { value_present = false }`; JS Map/objects have no block-taking `fetch`, so the key-presence probe is `has`/`hasOwnProperty` and the miss arm is the surrounding branch."
  },
  {
    "package": "activemodel",
    "tsFile": "attribute-set/builder.ts",
    "rubyName": "materialize",
    "call": "each_key",
    "reason": "builder.rb:153-154 walks `values.each_key`/`types.each_key`; `values` is a plain object and `types` a Map here, neither of which has Ruby's Hash#each_key — `Object.keys(...)`/`Map#keys()` is the only spelling (same gap as the `attributes` -> `each_key` row above)."
  }
]
