[
  {
    "package": "activemodel",
    "tsFile": "type/helpers/time-value.ts",
    "rubyName": "fast_string_to_time",
    "call": "new",
    "reason": "Reviewed per-body: Rails builds `::Time.new(string, in: \"UTC\")` (time_value.rb:76-98) and trails' `Time.new` (packages/date/src/time.ts:611) mirrors it, but `fastStringToTime` is seated on `Temporal.Instant` — the type its callers (type/date-time.ts:52, type/time.ts:72) and the ActiveModel time types read — so swapping the seat is a return-type change tracked by `fast-string-to-time-construct-through-time-new`. The parse arms are converged against MRI here."
  }
]
