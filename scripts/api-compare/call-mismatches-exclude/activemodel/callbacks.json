[
  {
    "package": "activemodel",
    "tsFile": "callbacks.ts",
    "rubyName": "define_model_callbacks",
    "call": "define_callbacks",
    "kind": "args",
    "rubyArgs": [
      "ref:callback",
      "ref:options"
    ],
    "reason": "Baseline (RFC 0126): pre-existing call-ARGUMENT divergence newly VISIBLE to the gate — `ownerRecordsNothing` suppressed every pair whose resolved owner is a bodyless declaration beside the file's top-level body (the trails mixin shape), so this body was never compared and never baselined. Pending per-body convergence review."
  }
]
