[
  {
    "package": "activemodel",
    "tsFile": "serialization.ts",
    "rubyName": "serializable_attributes",
    "call": "read_attribute_for_serialization",
    "kind": "args",
    "rubyArgs": [
      "ref:n"
    ],
    "reason": "serialization.rb:167 `alias :read_attribute_for_serialization :send` — it is `send` on the record, so trails renders it as the module-level `readAttributeForSerialization(record, key)` (an instance override still wins, serialization.ts:292-297). Receiver-as-first-argument, not an extra argument."
  }
]
