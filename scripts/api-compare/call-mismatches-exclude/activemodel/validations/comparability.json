[
  {
    "package": "activemodel",
    "tsFile": "validations/comparability.ts",
    "rubyName": "error_options",
    "call": "merge!",
    "kind": "args",
    "rubyArgs": [
      "kwargs{count=ref:optionValue,value=ref:value}"
    ],
    "reason": "comparability.rb:11 calls `Hash#merge!` ON the except-ed options hash; `@blazetrails/ruby-compat`'s `mergeBang` is a free function, so the receiver is the extra first argument. The `@missingRailsArgs` receipt cannot be spelled here — `error_options` resolves to the bodyless `Comparability` interface member, and interface JSDoc carries no tags into the extractor."
  }
]
