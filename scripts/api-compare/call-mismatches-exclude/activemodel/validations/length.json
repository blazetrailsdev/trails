[
  {
    "package": "activemodel",
    "tsFile": "validations/length.ts",
    "rubyName": "check_validity!",
    "call": "keys",
    "kind": "args",
    "rubyArgs": [
      "ref:options"
    ],
    "reason": "Language shortcoming: `CHECKS.keys & options.keys` (length.rb:30) intersects two key lists; the TS body filters `Object.keys(CHECKS)` on `this.options[key] !== undefined` instead, because an explicitly-passed `undefined` is Ruby's ABSENT kwarg — `Object.keys(this.options)` would count it present and accept `maximum: undefined`."
  }
]
