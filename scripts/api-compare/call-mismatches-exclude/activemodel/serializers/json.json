[
  {
    "package": "activemodel",
    "tsFile": "serializers/json.ts",
    "rubyName": "from_json",
    "call": "first",
    "reason": "`hash.values.first` (serializers/json.rb:148) reads Array#first off a plain JS array, which carries no Ruby Enumerable, so it is spelled `Object.values(hash)[0]`; surfaced once ruby-compat's Enumerable made `first` a known TS name."
  }
]
