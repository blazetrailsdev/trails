[
  {
    "package": "activerecord",
    "tsFile": "encryption/extended-deterministic-queries.ts",
    "rubyName": "install_support",
    "call": "include",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "encryption/extended-deterministic-queries.ts",
    "rubyName": "process_arguments",
    "call": "empty?",
    "reason": "Verified per-site (RFC 0106): `owner.deterministic_encrypted_attributes&.empty?` and `type.previous_types.empty?` (extended_deterministic_queries.rb:46,61) — `empty?` on a Ruby Array, whose faithful JS spelling is `xs.length === 0`. That emits no callee, so no TS call can ever credit the Ruby one. The gate flags it only because `empty?` maps onto the unrelated `ActiveRecord::Result.empty`, which takes arguments since it gained Rails' `async:` kwarg (result.rb:94-100) — nothing in the TS body was dropped."
  },
  {
    "package": "activerecord",
    "tsFile": "encryption/extended-deterministic-queries.ts",
    "rubyName": "scope_for_create",
    "call": "any?",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  }
]
