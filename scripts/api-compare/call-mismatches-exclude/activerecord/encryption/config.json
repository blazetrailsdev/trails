[
  {
    "package": "activerecord",
    "tsFile": "encryption/config.ts",
    "rubyName": "support_sha1_for_non_deterministic_encryption=",
    "call": "new",
    "kind": "args",
    "rubyArgs": [
      "kwargs{hashDigestClass=const:SHA1}"
    ],
    "reason": "encryption/config.rb:29 `KeyGenerator.new(hash_digest_class: OpenSSL::Digest::SHA1)` names a digest CLASS. node's crypto has no digest class to name — it selects the algorithm by string — so trails' `Config#hashDigestClass` is the algorithm name throughout (config.ts:56, :161) and the kwarg carries `\"SHA1\"`. Same kwarg, same key; only the value's language differs."
  }
]
