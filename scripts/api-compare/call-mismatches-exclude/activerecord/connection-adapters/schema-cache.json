[
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/schema-cache.ts",
    "rubyName": "read",
    "call": "open",
    "kind": "args",
    "rubyArgs": [
      "ref:filename"
    ],
    "reason": "Rails opens the .gz through `Zlib::GzipReader.open(filename)` (schema_cache.rb:246); the port opens the file itself in binary mode and hands the bytes to Gzip.decompress, so File.open takes the mode and block Zlib's one-argument open implies."
  }
]
