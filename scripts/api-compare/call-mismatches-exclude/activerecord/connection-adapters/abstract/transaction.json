[
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "before_commit_records",
    "call": "run_action_on_records",
    "reason": "Per-site verified (RFC 0106 wave 4b): transaction.rb's `before_commit_records` routes through `run_action_on_records`, which threads the `@run_commit_callbacks` flag; trails' beforeCommitRecords walks `this.records` directly because its callback dispatch is async and cannot be driven by the shared synchronous helper."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "begin_transaction",
    "call": "empty?",
    "reason": "Verified per-site (RFC 0106): `@stack.empty?` (transaction.rb:510) — `empty?` on a Ruby Array, whose faithful JS spelling is `xs.length === 0`. That emits no callee, so no TS call can ever credit the Ruby one. The gate flags it only because `empty?` maps onto the unrelated `ActiveRecord::Result.empty`, which takes arguments since it gained Rails' `async:` kwarg (result.rb:94-100) — nothing in the TS body was dropped."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "begin_transaction",
    "call": "size",
    "reason": "Per-site verified (RFC 0106 wave 4b): transaction.rb's `@stack.size` on a Ruby Array is `this._stack.length` on the JS array — `Array#size` is not a ported method name."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "commit_transaction",
    "call": "last",
    "reason": "Per-site verified (RFC 0106 wave 4b): Ruby's `@stack.last` is `this._stack[this._stack.length - 1]` on a JS array; `Array#last` is not a ported method name."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "current_transaction",
    "call": "last",
    "reason": "Verified per-site (RFC 0106): `@stack.last || NULL_TRANSACTION` (`connection_adapters/abstract/transaction.rb:662`) — the TS body reads the last stack slot and falls back to the same `NULL_TRANSACTION`. `first`/`last`/`size` are positional/property idioms with no JS call form, deliberately left uncredited by RFC 0092 (`positional-idiom-analogues`, see JS_ENUMERABLE_ALIASES' header comment) so the reason-text route is the sanctioned one; nothing was dropped from the TS body."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "open_transactions",
    "call": "size",
    "reason": "Verified per-site (RFC 0106): `@stack.size` (`connection_adapters/abstract/transaction.rb:658`) — the TS body is `this._stack.length`, the whole method. `first`/`last`/`size` are positional/property idioms with no JS call form, deliberately left uncredited by RFC 0092 (`positional-idiom-analogues`, see JS_ENUMERABLE_ALIASES' header comment) so the reason-text route is the sanctioned one; nothing was dropped from the TS body."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract/transaction.ts",
    "rubyName": "rollback_transaction",
    "call": "last",
    "reason": "Per-site verified (RFC 0106 wave 4b): Ruby's `@stack.last` is `this._stack[this._stack.length - 1]` on a JS array; `Array#last` is not a ported method name."
  }
]
