[
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/pool-config.ts",
    "rubyName": "discard_pools!",
    "call": "each_key",
    "reason": "Per-site verified (RFC 0106 wave 4b): pool_config.rb:20 is `INSTANCES.each_key(&:discard_pool!)` over a Ruby Hash used as a set; trails' INSTANCES is a Set of WeakRefs walked with `for..of` (pool-config.ts:287-305), which also has to skip collected refs. `Hash#each_key` has no ported analogue."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/pool-config.ts",
    "rubyName": "disconnect_all!",
    "call": "each_key",
    "reason": "Per-site verified (RFC 0106 wave 4b): pool_config.rb:24 is the same `INSTANCES.each_key { ... }` idiom over the WeakRef Set (pool-config.ts:306-320)."
  }
]
