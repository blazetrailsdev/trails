[
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/sqlite3/database-statements.ts",
    "rubyName": "default_insert_value",
    "call": "sql",
    "reason": "Per-site verified (RFC 0106 wave 4b): sqlite3/database_statements.rb:140 is `Arel.sql(column.default_function)`; trails calls the same `Arel.sql` export, imported as `arelSql` because the surrounding bodies bind `sql` as a parameter name (sqlite3/database-statements.ts:11)."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/sqlite3/database-statements.ts",
    "rubyName": "returning_column_values",
    "call": "first",
    "reason": "Per-site verified (RFC 0106 wave 4b): sqlite3/database_statements.rb:136 is `result.rows.first`, which is `result.rows[0]` on a JS array; `Array#first` is not a ported method name."
  }
]
