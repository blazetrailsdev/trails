[
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "all_attributes?",
    "call": "empty?",
    "reason": "Verified per-site (RFC 0106): `(column_names.map(&:to_s) - model.attribute_names - model.attribute_aliases.keys).empty?` (calculations.rb:427) — `empty?` on a Ruby Array, whose faithful JS spelling is `xs.length === 0`. That emits no callee, so no TS call can ever credit the Ruby one. The gate flags it only because `empty?` maps onto the unrelated `ActiveRecord::Result.empty`, which takes arguments since it gained Rails' `async:` kwarg (result.rb:94-100) — nothing in the TS body was dropped."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "calculate",
    "call": "apply_join_dependency",
    "reason": "Per-entry verified: Rails calculations.rb:232 `relation = apply_join_dependency`. Rails' apply_join_dependency EXECUTES `distinct_relation_for_primary_key` for a limited collection join, which a synchronous TS method cannot do, so `calculate` calls the awaitable block-form sibling `Relation#_applyJoinDependencyAsync` (finder_methods.rb:456-488, block arm) instead; the leading underscore keeps it off the compare population."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "calculate",
    "call": "new",
    "kind": "args",
    "rubyArgs": [
      "ref:result"
    ],
    "reason": "Per-entry verified: Rails calculations.rb:224/228 `@async ? Promise::Complete.new(result) : result` — trails has no `@async` relation flag, so only the synchronous arm is ported and the sole `new` in the body is the `Hash.new` (`new Map()`) the grouped result uses, which takes no arguments."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "calculate",
    "call": "table",
    "reason": "Per-entry verified: Rails calculations.rb:238 `Array(model.primary_key || table[Arel.star])` reads `table` only for the no-primary-key fallback; the port spells that fallback as the `*` literal it compiles to, so no table read happens on the branch."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "execute_grouped_calculation",
    "call": "fetch",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "execute_grouped_calculation",
    "call": "order:constructor,withConnection",
    "reason": "Verified per-site (RFC 0106): calculations.rb:527 opens `model.with_connection do |connection|` around the whole remainder of the body, so `with_connection` precedes `ColumnAliasTracker.new` (:528); trails calls it at the `select_all` (:556) instead, because the calculation entry points already run inside `inQueryConnection`'s `with_connection`. Hoisting it to :527 is a whole-body reindent and rides `converge-execute-grouped-calculation-body-to-rails-source-order`."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "execute_grouped_calculation",
    "call": "type_for",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "execute_simple_calculation",
    "call": "first",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "execute_simple_calculation",
    "call": "wrap",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "ids",
    "call": "new",
    "reason": "Rails' `@async` arms wrap the result in `Promise::Complete.new` (calculations.rb:294, :306, :382); trails has no ActiveRecord::Promise — every reader is already a native Promise — so the body has no constructor at that point and the first `new` it does reach lands later."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "lookup_cast_type_from_join_dependencies",
    "call": "fetch",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "pluck",
    "call": "empty",
    "kind": "args",
    "rubyArgs": [
      "kwargs{async=ref:async}"
    ],
    "reason": "`ActiveRecord::Result.empty(async: @async)` (calculations.rb:320): trails has no `@async` flag — every reader is already a native Promise — so `Result.empty()` takes no kwarg."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "pluck",
    "call": "order:loaded,constructor",
    "reason": "Rails' `@async` arms wrap the result in `Promise::Complete.new` (calculations.rb:294, :306, :382); trails has no ActiveRecord::Promise — every reader is already a native Promise — so the body has no constructor at that point and the first `new` it does reach lands later."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "select_for_count",
    "call": "empty?",
    "reason": "Verified per-site (RFC 0106): `select_values.empty?` (calculations.rb:646) — `empty?` on a Ruby Array, whose faithful JS spelling is `xs.length === 0`. That emits no callee, so no TS call can ever credit the Ruby one. The gate flags it only because `empty?` maps onto the unrelated `ActiveRecord::Result.empty`, which takes arguments since it gained Rails' `async:` kwarg (result.rb:94-100) — nothing in the TS body was dropped."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "type_cast_pluck_values",
    "call": "fetch",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "type_cast_pluck_values",
    "call": "size",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation/calculations.ts",
    "rubyName": "type_for",
    "call": "last",
    "reason": "Baseline (RFC 0047): wide call-set flag seeded when the wide ratchet landed; bucket (b) equivalent or (c) noise pending per-cluster burndown review."
  }
]
