[
  {
    "package": "activerecord",
    "tsFile": "associations/builder/belongs-to.ts",
    "rubyName": "define_validations",
    "call": "delete",
    "reason": "Ruby Hash#delete returns the DELETED value: `!reflection.options.delete(:required)` (builder/belongs_to.rb:59) ports to reading `options.required` and then `delete options.required` — JS `delete` returns a boolean, so the two steps cannot fold."
  },
  {
    "package": "activerecord",
    "tsFile": "associations/builder/belongs-to.ts",
    "rubyName": "touch_record",
    "call": "first",
    "reason": "Ruby Array#first on the [old, new] change pair: `changes[foreign_key].first` (builder/belongs_to.rb:45) ports to `change[0]`."
  }
]
