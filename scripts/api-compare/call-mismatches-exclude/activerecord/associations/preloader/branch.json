[
  {
    "package": "activerecord",
    "tsFile": "associations/preloader/branch.ts",
    "rubyName": "grouped_records",
    "call": "association",
    "kind": "args",
    "rubyArgs": [],
    "reason": "branch.rb:7,84 — Rails' bare `association` is `attr_reader :association`, so `record.association(association)` contains a nested 0-arg reader call; trails' `this.association` is a field read, not a call node. Same receiver, same argument — extractor pairing artifact."
  },
  {
    "package": "activerecord",
    "tsFile": "associations/preloader/branch.ts",
    "rubyName": "preloaders_for_reflection",
    "call": "association",
    "kind": "args",
    "rubyArgs": [],
    "reason": "branch.rb:7,93 — Rails' bare `association` is `attr_reader :association`, so `record.association(association)` contains a nested 0-arg reader call; trails' `this.association` is a field read, not a call node. Same receiver, same argument — extractor pairing artifact."
  }
]
