[
  {
    "package": "activerecord",
    "tsFile": "associations/alias-tracker.ts",
    "rubyName": "create",
    "call": "new",
    "kind": "args",
    "rubyArgs": [
      "num:0"
    ],
    "reason": "alias_tracker.rb:14 builds `Hash.new(0)` — a hash with a DEFAULT-VALUE proc. A JS Map has no default, so the port passes the proc itself to the `AliasCounts` Map subclass that carries it; the literal `0` Rails passes has no counterpart argument."
  }
]
