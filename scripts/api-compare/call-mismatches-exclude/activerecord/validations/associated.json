[
  {
    "package": "activerecord",
    "tsFile": "validations/associated.ts",
    "rubyName": "validate_each",
    "call": "any?",
    "reason": "Verified per-site (RFC 0106): `.any?` on the rejected list (`associated.rb:9`) — folded into the same ordered async loop as `reject`; see that row."
  },
  {
    "package": "activerecord",
    "tsFile": "validations/associated.ts",
    "rubyName": "validate_each",
    "call": "merge",
    "reason": "Verified per-site (RFC 0106): `options.merge(value: value)` (`associated.rb:10`) — a non-mutating Hash merge is an object spread in TS."
  },
  {
    "package": "activerecord",
    "tsFile": "validations/associated.ts",
    "rubyName": "validate_each",
    "call": "reject",
    "reason": "Verified per-site (RFC 0106): `Array(value).reject { |a| valid_object?(a, context) }` (`associated.rb:9`) — `valid_object?` is async since RFC 0063 and JS has no async-predicate `reject`, so the reject/any? pair is one ordered loop over the same predicate (which also preserves Rails' sequential `valid?` semantics)."
  }
]
