[
  {
    "package": "activerecord",
    "tsFile": "statement-cache.ts",
    "rubyName": "execute",
    "call": "async_find_by_sql",
    "reason": "Verified per-site (RFC 0106 wave 4g): `@model.async_find_by_sql(...)` (statement_cache.rb:149) is the `async: true` arm of `execute`. trails' `execute` takes no `async` kwarg because no caller can pass one — `Association#find_target`'s `sc.execute(binds, c, async: async)` (association.rb:269) has no ported counterpart while `ActiveRecord::Promise` is unmodelled. Tracked by story `port-promise-complete-for-async-loaded-arms` (RFC 0023); adding the arm before then would be an unreachable code path."
  },
  {
    "package": "activerecord",
    "tsFile": "statement-cache.ts",
    "rubyName": "execute",
    "call": "wrap",
    "reason": "Verified per-site (RFC 0106 wave 4g): `async ? Promise.wrap([]) : []` in the `rescue ::RangeError` (statement_cache.rb:154). trails returns the bare `[]` because the `async` arm does not exist — `ActiveRecord::Promise` is unported, same gap as the `async_find_by_sql` row above, tracked by story `port-promise-complete-for-async-loaded-arms` (RFC 0023)."
  }
]
