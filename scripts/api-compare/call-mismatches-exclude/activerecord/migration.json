[
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "call",
    "call": "build_watcher",
    "reason": "Rails memoizes a `FileUpdateChecker` over the migration paths (migration.rb:658,675-680); trails registers migrations programmatically, so `CheckPending#call` checks the migrator directly and has no watcher to build."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "call",
    "call": "execute",
    "reason": "Rails' `@watcher.execute` is the FileUpdateChecker hop; trails' CheckPending has no watcher and checks pending migrations directly (same cluster as the baselined `synchronize` / `build_watcher`)."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "change_table",
    "call": "compatible_table_definition",
    "reason": "Rails' `Migration::Current` wrappers (migration.rb:580-609) exist only so a `Migration[x.y]` compatibility class can override the identity hook `compatible_table_definition` (migration.rb:612-614). Version compatibility is out of scope for the port, so the wrappers are not ported and the base `change_table` is what pairs here."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "copy",
    "call": "call",
    "reason": "Ruby's `options[:on_skip].call(scope, migration)` is a Proc invocation; TS calls the callback directly as `options.onSkip(scope, source)`, which has no `.call` token."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "create_join_table",
    "call": "compatible_table_definition",
    "reason": "Rails' `Migration::Current` wrappers (migration.rb:580-609) exist only so a `Migration[x.y]` compatibility class can override the identity hook `compatible_table_definition` (migration.rb:612-614). Version compatibility is out of scope for the port, so the wrappers are not ported and the base `create_join_table` is what pairs here."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "create_table",
    "call": "compatible_table_definition",
    "reason": "Rails' `Migration::Current` wrappers (migration.rb:580-609) exist only so a `Migration[x.y]` compatibility class can override the identity hook `compatible_table_definition` (migration.rb:612-614). Version compatibility is out of scope for the port, so the wrappers are not ported and the base `create_table` is what pairs here."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "current_environment",
    "call": "call",
    "reason": "Verified per-site (RFC 0106): `ActiveRecord::ConnectionHandling::DEFAULT_ENV.call` (`migration.rb:1341`) — same unported `DEFAULT_ENV` Proc as `default_env`; filed as `port-connection-handling-default-env-proc`."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "drop_table",
    "call": "compatible_table_definition",
    "reason": "Rails' `Migration::Current` wrappers (migration.rb:580-609) exist only so a `Migration[x.y]` compatibility class can override the identity hook `compatible_table_definition` (migration.rb:612-614). Version compatibility is out of scope for the port, so the wrappers are not ported and the base `drop_table` is what pairs here."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "env",
    "call": "call",
    "reason": "`ActiveRecord::ConnectionHandling::DEFAULT_ENV.call` (migration.rb:773) invokes a Proc; the port's DEFAULT_ENV is a function it calls directly, so there is no `call` callee to record."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "migrate",
    "call": "with_connection",
    "reason": "`DatabaseTasks.migration_connection.pool.with_connection` (migration.rb:973) checks a connection out for the exec_migration block; trails migrations run against the already-seated `this.connection` (RFC 0073's permanent-checkout flip is where this converges)."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "needs_migration?",
    "call": "size",
    "reason": "Ruby `Array#size` (migration.rb:1296); JS reads `.length`, a property, not a call."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "parse_migration_filename",
    "call": "first",
    "reason": "Ruby `Array#first` on the `scan` result (migration.rb:1374); JS indexes with `[0]`, which records no callee."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "validate",
    "call": "find",
    "reason": "Ruby `Enumerable#find` over `group_by(&:name)` (migration.rb:1558,1561); the port counts names and versions in two Maps and walks the list in order, so the first duplicate reported matches Rails' first-occurrence semantics."
  }
]
