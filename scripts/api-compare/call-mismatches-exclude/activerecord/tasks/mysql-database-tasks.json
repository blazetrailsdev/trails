[
  {
    "package": "activerecord",
    "tsFile": "tasks/mysql-database-tasks.ts",
    "rubyName": "configuration_hash_without_database",
    "call": "merge",
    "reason": "Verified per-site (RFC 0106): `configuration_hash.merge(database: nil)` (`mysql_database_tasks.rb:80`) — a non-mutating Hash merge is an object spread in TS, which is not a `merge` call the comparator can credit."
  },
  {
    "package": "activerecord",
    "tsFile": "tasks/mysql-database-tasks.ts",
    "rubyName": "creation_options",
    "call": "new",
    "reason": "Verified per-site (RFC 0106): `Hash.new.tap { |options| ... }` (`mysql_database_tasks.rb:84`) — a fresh Hash is an object literal in TS; `Hash.new` has no TS call spelling."
  },
  {
    "package": "activerecord",
    "tsFile": "tasks/mysql-database-tasks.ts",
    "rubyName": "purge",
    "call": "recreate_database",
    "reason": "Verified per-site (RFC 0106): tracked as RFC 0051's `mysql-purge-does-not-call-recreate-database`; the deviation and its measured reason (preserving the slot database's charset/collation, which `creation_options` cannot see) are justified at the call site in `purge`."
  }
]
