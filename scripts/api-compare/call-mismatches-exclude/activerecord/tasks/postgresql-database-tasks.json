[
  {
    "package": "activerecord",
    "tsFile": "tasks/postgresql-database-tasks.ts",
    "rubyName": "create",
    "call": "merge",
    "reason": "Verified per-site (RFC 0106): `configuration_hash.merge(encoding: encoding)` (`postgresql_database_tasks.rb:23`) — a non-mutating Hash merge is an object spread in TS, which is not a `merge` call the comparator can credit."
  },
  {
    "package": "activerecord",
    "tsFile": "tasks/postgresql-database-tasks.ts",
    "rubyName": "public_schema_config",
    "call": "merge",
    "reason": "Verified per-site (RFC 0106): `configuration_hash.merge(database: \"postgres\", schema_search_path: \"public\")` (`postgresql_database_tasks.rb:103`) — a non-mutating Hash merge is an object spread in TS."
  },
  {
    "package": "activerecord",
    "tsFile": "tasks/postgresql-database-tasks.ts",
    "rubyName": "remove_sql_header_comments",
    "call": "open",
    "reason": "Verified per-site (RFC 0106): `Tempfile.open(\"uncommented_structure.sql\")` (`postgresql_database_tasks.rb:132`) — the fs port has no Tempfile analogue, so the body makes a temp dir with `mkdtempSync` and writes into it. `open` has no TS call spelling here."
  },
  {
    "package": "activerecord",
    "tsFile": "tasks/postgresql-database-tasks.ts",
    "rubyName": "structure_dump",
    "call": "open",
    "reason": "Verified per-site (RFC 0106): `File.open(filename, \"a\") { |f| f << ... }` (`postgresql_database_tasks.rb:77`) — the fs port spells an append `appendFileSync`, with no open-file handle to model the block form."
  }
]
