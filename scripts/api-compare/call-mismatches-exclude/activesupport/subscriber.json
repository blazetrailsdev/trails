[
  {
    "package": "activesupport",
    "tsFile": "subscriber.ts",
    "rubyName": "add_event_subscriber",
    "call": "subscribe",
    "kind": "args",
    "rubyArgs": [
      "ref:pattern",
      "ref:subscriber"
    ],
    "reason": "Surfaced by the leading-underscore call candidate (RFC 0106): the widened candidate list opened the ported-with-args gate on this pre-existing divergence, which it had kept invisible. subscriber.rb:94 `notifier.subscribe(pattern, subscriber)` passes the Subscriber object, which Notifications invokes through `#call`; the JS notifier takes a callable, so trails passes `(e) => sub.call(e)` — the same Proc-vs-callable substitution as the `log-subscriber.ts silenced? -> call` row. Filed for a deliberate ruling as 0106/converge-log-subscriber-transform-keys-and-proc."
  }
]
