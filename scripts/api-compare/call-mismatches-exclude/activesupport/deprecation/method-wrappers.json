[
  {
    "package": "activesupport",
    "tsFile": "deprecation/method-wrappers.ts",
    "rubyName": "deprecate_methods",
    "call": "define_method",
    "reason": "method_wrappers.rb:44-64 keeps both arms and the port keeps both — the owned-method arm reassigns over the captured implementation (Ruby's `redefine_method` over `instance_method`), the missing-method arm defines one that warns and then resolves the inherited implementation at call time (Ruby's `Module.new` + `define_method` + `prepend`, whose body is `super`). What has no JS counterpart is the module machinery itself: an own property shadows the prototype chain the way a prepended module shadows its ancestors, so there is no module to construct and no `define_method` / `redefine_method` API to call."
  },
  {
    "package": "activesupport",
    "tsFile": "deprecation/method-wrappers.ts",
    "rubyName": "deprecate_methods",
    "call": "new",
    "reason": "method_wrappers.rb:44-64 keeps both arms and the port keeps both — the owned-method arm reassigns over the captured implementation (Ruby's `redefine_method` over `instance_method`), the missing-method arm defines one that warns and then resolves the inherited implementation at call time (Ruby's `Module.new` + `define_method` + `prepend`, whose body is `super`). What has no JS counterpart is the module machinery itself: an own property shadows the prototype chain the way a prepended module shadows its ancestors, so there is no module to construct and no `define_method` / `redefine_method` API to call."
  },
  {
    "package": "activesupport",
    "tsFile": "deprecation/method-wrappers.ts",
    "rubyName": "deprecate_methods",
    "call": "redefine_method",
    "reason": "method_wrappers.rb:44-64 keeps both arms and the port keeps both — the owned-method arm reassigns over the captured implementation (Ruby's `redefine_method` over `instance_method`), the missing-method arm defines one that warns and then resolves the inherited implementation at call time (Ruby's `Module.new` + `define_method` + `prepend`, whose body is `super`). What has no JS counterpart is the module machinery itself: an own property shadows the prototype chain the way a prepended module shadows its ancestors, so there is no module to construct and no `define_method` / `redefine_method` API to call."
  }
]
