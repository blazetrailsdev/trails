[
  {
    "package": "activesupport",
    "tsFile": "duration.ts",
    "rubyName": "iso8601",
    "call": "new",
    "reason": "Rails builds the string via ISO8601Serializer.new(self, precision:) (duration.rb:474); trails inlines the serializer in iso8601 (the `serialize` half of the same call is already baselined above)."
  },
  {
    "package": "activesupport",
    "tsFile": "duration.ts",
    "rubyName": "iso8601",
    "call": "serialize",
    "reason": "Rails builds the string via ISO8601Serializer#serialize; trails inlines it in iso8601 (the `new` half of the same call is already baselined above)."
  },
  {
    "package": "activesupport",
    "tsFile": "duration.ts",
    "rubyName": "parse",
    "call": "order:calculateTotalSeconds,constructor",
    "reason": "Rails' Duration.parse delegates the string to ISO8601Parser and raises through it (duration.rb:144-147); trails inlines that parser, so parse's own invalid-input guards `throw new Error(...)` and the extractor reads each as a `constructor` call ahead of calculateTotalSeconds. The value seat itself is filled in Rails' order — calculateTotalSeconds(parts), then new Duration(value, parts). Converges with the inlined parser (the iso8601 new/serialize rows track the serializer half)."
  }
]
