[
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "_deep_transform_keys_in_object",
    "call": "new",
    "reason": "Ruby's `self.class.new` builds the empty accumulator of the receiver's Hash subclass; a plain JS object literal `{}` is that accumulator and there is no constructor to call."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "deep_stringify_keys",
    "call": "deep_transform_keys",
    "kind": "args",
    "rubyArgs": [],
    "reason": "Baseline (RFC 0095): call-ARGUMENT shape divergence seeded when the argument ratchet landed — the port makes the call Rails makes with a different argument count, order, literal value or kwarg key; pending per-body convergence review."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "deep_stringify_keys!",
    "call": "deep_transform_keys!",
    "kind": "args",
    "rubyArgs": [],
    "reason": "Ruby passes the key transform as a BLOCK, which contributes no positional argument; TypeScript has no block syntax, so the port passes it as a trailing function parameter (and the receiver as the leading one, the settled trails core_ext convention). Same call, same order, one extra positional."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "deep_symbolize_keys!",
    "call": "deep_transform_keys!",
    "kind": "args",
    "rubyArgs": [],
    "reason": "Ruby passes the key transform as a BLOCK, which contributes no positional argument; TypeScript has no block syntax, so the port passes it as a trailing function parameter (and the receiver as the leading one, the settled trails core_ext convention). Same call, same order, one extra positional."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "except!",
    "call": "delete",
    "reason": "Ruby's `Hash#delete(key)` is a method call; JS's key removal is the `delete obj[key]` operator, which is the same operation with no callee to record."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "extract_options!",
    "call": "extractable_options?",
    "kind": "args",
    "rubyArgs": [],
    "reason": "Ruby calls `last.extractable_options?` on the receiver; TS cannot reopen Hash, so `isExtractableOptions(last)` takes the receiver as its first argument (the `core-ext/array/access.ts` idiom) — one argument where Ruby passes none."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "extract_options!",
    "call": "last",
    "reason": "`last` is Ruby's Array#last; `args[args.length - 1]` is an index expression, not a call node, so the read has no call to match."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "reverse_merge!",
    "call": "replace",
    "reason": "Ruby's `Hash#replace` swaps a hash's contents in place; JS objects have no such method, so the port clears the own keys and `Object.assign`s the merged result — the same in-place replacement."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "reverse_merge!",
    "call": "reverse_merge",
    "kind": "args",
    "rubyArgs": [
      "ref:otherHash"
    ],
    "reason": "Ruby's `reverse_merge(other_hash)` is called on the receiver; trails' core_ext functions take the receiver as the leading positional, so the same call carries one extra argument."
  },
  {
    "package": "activesupport",
    "tsFile": "hash-utils.ts",
    "rubyName": "slice!",
    "call": "replace",
    "reason": "Ruby's `Hash#replace` swaps a hash's contents in place; JS objects have no such method, so the port clears the own keys and `Object.assign`s the sliced result — the same in-place replacement."
  }
]
