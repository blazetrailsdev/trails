[
  {
    "package": "activesupport",
    "tsFile": "class-attribute.ts",
    "rubyName": "class_attribute",
    "call": "caller_locations",
    "reason": "Rails threads `location: caller_locations(1, 1).first` into the `module_eval`ed definitions (class/attribute.rb:141) so a raise points at the declaring line; trails generates real JS accessors, which carry a real stack, and has no `module_eval` file/line to attribute. Same omission already baselined for `delegate` / `mattr_accessor` in module-ext.json."
  },
  {
    "package": "activesupport",
    "tsFile": "class-attribute.ts",
    "rubyName": "class_attribute",
    "call": "class_eval",
    "reason": "Homonym, not an omission: `class_eval` at class/attribute.rb:133 is Ruby's Module#class_eval over a joined source string, which trails has no equivalent for (it defines real JS accessors); the ported name the gate matches on is ActiveSupport::CodeGenerator#class_eval, a different method this body never calls."
  },
  {
    "package": "activesupport",
    "tsFile": "class-attribute.ts",
    "rubyName": "class_attribute",
    "call": "first",
    "reason": "Part of the same omission — `first` is only there to pick the `caller_locations(1, 1)` frame."
  }
]
