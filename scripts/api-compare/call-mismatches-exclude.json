[
  {
    "package": "activerecord",
    "tsFile": "associations/collection-association.ts",
    "rubyName": "concat",
    "call": "transaction",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "associations/collection-association.ts",
    "rubyName": "replace",
    "call": "transaction",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "associations/has-one-association.ts",
    "rubyName": "replace",
    "call": "save",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): trails splits Rails' synchronous `HasOneAssociation#replace` (which saves the new record inside `transaction_if(save)` for a persisted owner -- has_one_association.rb:66-79) into a sync `replace` that records `_pendingReplace` plus an async `persistReplace` that performs `pending.record.save()`. The awaitable `writer` returns `persistReplace()` immediately for a persisted owner (has-one-association.ts:52-55 -- the Rails-faithful immediate persist); the sync property-setter `queueWrite`/new-owner path defers the same `persistReplace` to the owner's next save via `flushPendingReplaces`. Either way the save lands in `persistReplace`, not literally inside `replace`, so the extractor flags it -- false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "attribute-methods.ts",
    "rubyName": "_create_record",
    "call": "changes_applied",
    "reason": "Confirmed equivalent (RFC 0044, dirty-tracking-changes-applied-cluster): the attribute-methods.ts `_createRecord` delegate forwards to dirty.ts `_createRecord`, but the live create path runs through callbacks.ts `_createRecord`, which awaits `_performInsert()` then calls `changesApplied()` (callbacks.ts:306-313) -- mirroring Rails AttributeMethods::Dirty#_create_record (super; changes_applied). dirty.test.ts 'attribute changes' asserts `catchphrase_changed?` is false after saving a new record. The extractor does not follow the delegation into callbacks.ts -- false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "attribute-methods.ts",
    "rubyName": "_touch_row",
    "call": "changes_applied",
    "reason": "Confirmed equivalent (RFC 0044, dirty-tracking-changes-applied-cluster): the live touch path is timestamp.ts `touchRow`, which after the UPDATE runs Rails' AttributeMethods::Dirty#_touch_row dirty-clearing -- stashing non-touched changes, reverting them so `changesApplied()` snapshots the pre-change value, then re-writing them (timestamp.ts, after the `_triggerUpdateCallback` assignment). dirty.ts `_touchRow` (the call-set parity surface) contains the same `changesApplied`. timestamp.test.ts 'touching a record updates its timestamp' asserts an unrelated in-memory change survives touch while the touched timestamp's dirty flag clears. The extractor does not follow the delegation -- false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "attribute-methods.ts",
    "rubyName": "_touch_row",
    "call": "clear_attribute_changes",
    "reason": "Confirmed equivalent (RFC 0044, dirty-tracking-changes-applied-cluster): the live touch path (timestamp.ts `touchRow`) calls `clearAttributeChanges(touchCols)` on the `_skipDirtyTracking` branch (set by touch_later), mirroring Rails _touch_row's `clear_attribute_changes(@_touch_attr_names)` guard; the non-skip branch instead clears per-attribute via `clearAttributeChange` + `changesApplied`. dirty.ts `_touchRow` (the call-set parity surface) carries the same `clearAttributeChanges`. The extractor does not follow the delegation -- false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "attribute-methods.ts",
    "rubyName": "_update_record",
    "call": "changes_applied",
    "reason": "Confirmed equivalent (RFC 0044, dirty-tracking-changes-applied-cluster): the attribute-methods.ts `_updateRecord` delegate forwards to dirty.ts `_updateRecord`, but the live update path runs through callbacks.ts `_updateRecord`, which awaits `_performUpdate()` then calls `changesApplied()` (callbacks.ts:357-366) -- mirroring Rails AttributeMethods::Dirty#_update_record (super; changes_applied). The extractor does not follow the delegation into callbacks.ts -- false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "autosave-association.ts",
    "rubyName": "save_belongs_to_association",
    "call": "destroy",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): `saveBelongsToAssociation` delegates to `_autosaveBelongsTo`, which destroys a marked-for-destruction persisted target via `assocRecord.destroy()` after nulling the owner FK, mirroring save_belongs_to_association (autosave_association.rb:544-547). The extractor does not follow the delegation into the helper — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "autosave-association.ts",
    "rubyName": "save_belongs_to_association",
    "call": "save",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): `saveBelongsToAssociation` delegates to `_autosaveBelongsTo`, which saves the new/changed target via `assocRecord.save({validate:!autosave})`, mirroring save_belongs_to_association (autosave_association.rb:553). The extractor does not follow the delegation into the helper — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "autosave-association.ts",
    "rubyName": "save_collection_association",
    "call": "save",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): `saveCollectionAssociation` delegates to the `autosaveHasMany` helper, which saves new/changed children via `child.save({validate:false})` (through `_insertCollectionRecord`/`insertRecord`), mirroring save_collection_association (autosave_association.rb:446-456). The call-set extractor scopes to the Ruby-named function body and does not follow the delegation, so the save reads as missing — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "autosave-association.ts",
    "rubyName": "save_has_one_association",
    "call": "destroy",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): `saveHasOneAssociation` delegates to `autosaveHasOne`, which destroys a marked-for-destruction persisted child via `childRecord.destroy()`, mirroring save_has_one_association (autosave_association.rb:483). The extractor does not follow the delegation into the helper — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "autosave-association.ts",
    "rubyName": "save_has_one_association",
    "call": "save",
    "reason": "Confirmed equivalent (RFC 0044, autosave-association-save-destroy-cluster): `saveHasOneAssociation` delegates to `autosaveHasOne`, which saves the changed child via `childRecord.save({validate:false})`, mirroring save_has_one_association (autosave_association.rb:503). The extractor does not follow the delegation into the helper — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "base.ts",
    "rubyName": "update",
    "call": "save",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "base.ts",
    "rubyName": "update!",
    "call": "save!",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/abstract-mysql-adapter.ts",
    "rubyName": "mismatched_foreign_key",
    "call": "update",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "connection-adapters/sqlite3-adapter.ts",
    "rubyName": "alter_table",
    "call": "transaction",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "internal-metadata.ts",
    "rubyName": "update_entry",
    "call": "update",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "locking/pessimistic.ts",
    "rubyName": "with_lock",
    "call": "lock!",
    "reason": "Confirmed equivalent (RFC 0044, touch-and-pessimistic-locking-cluster): `withLock` is the port of `with_lock` and, inside the wrapping `transaction`, calls `lockBang.call(instance, lockClause)` — `lockBang` IS the `lock!` port (pessimistic.ts `InstanceMethods.lockBang`), mirroring pessimistic.rb's `transaction { lock!(lock); yield }`. The extractor keys on the Ruby name `lock!` and does not map it to the `lockBang` identifier reached via `.call`, so it reads as missing — false positive, no omission."
  },
  {
    "package": "activerecord",
    "tsFile": "migration.ts",
    "rubyName": "ddl_transaction",
    "call": "transaction",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "nested-attributes.ts",
    "rubyName": "accepts_nested_attributes_for",
    "call": "update",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "relation.ts",
    "rubyName": "update_all",
    "call": "update",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "secure-token.ts",
    "rubyName": "has_secure_token",
    "call": "update!",
    "reason": "Baseline (RFC 0044): pre-existing call-set flag inherited when the ratchet landed; pending per-cluster burndown review."
  },
  {
    "package": "activerecord",
    "tsFile": "touch-later.ts",
    "rubyName": "touch_deferred_attributes",
    "call": "touch",
    "reason": "Confirmed equivalent (RFC 0044, touch-and-pessimistic-locking-cluster): Rails `touch_deferred_attributes` calls `touch(time: @_touch_time)`, where `TouchLater#touch` merges the still-set `@_defer_touch_attrs` and delegates to `super` (timestamp.rb `touch`). trails `touchDeferredAttributes` performs that merge inline — it clears the deferred state then calls `timestampTouch.call(this, { time }, ...deferredAttrs)` (the timestamp.ts `touch`, i.e. the same `super`) with `_skipDirtyTracking` set, landing the identical single UPDATE. The extractor keys on the Ruby name `touch` and does not follow the aliased `timestampTouch` import — false positive, no omission."
  }
]
