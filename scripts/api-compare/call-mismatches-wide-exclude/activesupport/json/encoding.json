[
  {
    "package": "activesupport",
    "tsFile": "json/encoding.ts",
    "rubyName": "encode",
    "call": "fetch",
    "reason": "Ruby `@options.fetch(:escape_html_entities, Encoding.escape_html_entities_in_json)` (encoding.rb:62). TypeScript objects have no `fetch`; `??` is exactly fetch's semantics here because the key holds a boolean or is absent, and `??` substitutes only for null/undefined — a stored `false` is returned, not defaulted."
  },
  {
    "package": "activesupport",
    "tsFile": "json/encoding.ts",
    "rubyName": "stringify",
    "call": "generate",
    "reason": "Ruby `::JSON.generate(jsonified, quirks_mode: true, max_nesting: false)` (encoding.rb:106). The JavaScript analogue is spelled `JSON.stringify`; both kwargs are JSON.stringify's only behaviour (bare scalar roots, no nesting limit), so the call is the same operation under the language's name."
  }
]
