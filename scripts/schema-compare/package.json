{
  "private": true,
  "type": "module",
  "description": "Marks the schema-compare CLI as ESM. The repo root has no \"type\", so without this tsx loads compare.ts as CommonJS and transforms every workspace dist/ file it pulls in to CJS too — which fails on the top-level await in @blazetrails/activesupport's dist/yaml.js, and loads a second, unseated copy of activerecord's namespaces.js."
}
