<h1>#<%= hashtag.name %></h1>
<p class="muted"><%= tweets.length %> tweets</p>

<% for (const tweet of tweets) { %>
  <%= render({ partial: "tweets/tweet", locals: { tweet: tweet, isLoggedIn: isLoggedIn, currentUser: currentUser } }) %>
<% } %>
